Отлично! Теперь давайте пошагово настроим и создадим ваше мобильное приложение в Apache Cordova.

## 📱 Пошаговая инструкция:

### Шаг 1: Установка необходимых компонентов

Откройте командную строку (cmd) или PowerShell и выполните следующие команды:

```bash
# Проверьте, установлен ли Node.js
node --version

# Если не установлен, скачайте с https://nodejs.org/

# Установите Apache Cordova глобально
npm install -g cordova

# Проверьте установку
cordova --version
```

### Шаг 2: Создание проекта Cordova

Перейдите в папку, где хотите создать проект, и выполните:

```bash
# Создайте новый проект
cordova create GeoCalcApp com.geocalc.app GeoCalc

# Перейдите в папку проекта
cd GeoCalcApp

# Добавьте платформу Android (или iOS для Mac)
cordova platform add android

# Для браузерной отладки (опционально)
cordova platform add browser
```

### Шаг 3: Замена файлов проекта

Теперь нужно заменить стандартные файлы Cordova на наш калькулятор. Я создам улучшенную версию с информацией об авторе:

**1. Откройте папку проекта:** `GeoCalcApp/www/`

**2. Замените содержимое файла `index.html`** на следующий код:**3. Создайте файл `css/index.css`** в папке `www/css/`:**4. Создайте файл `js/index.js`** в папке `www/js/`:### Шаг 4: Настройка конфигурации приложения

**Откройте файл `config.xml`** в корне проекта и замените его содержимое:

```xml
<?xml version='1.0' encoding='utf-8'?>
<widget id="com.geocalc.app" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0">
    <name>GeoCalc Pro</name>
    <description>
        Профессиональный калькулятор геометрических фигур для расчета периметров, площадей и объёмов.
    </description>
    <author email="student@example.com" href="https://geocalc.app">
        Студент ИСТ-33
    </author>
    <content src="index.html" />
    <access origin="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <platform name="android">
        <allow-intent href="market:*" />
        <icon density="ldpi" src="res/icon/android/ldpi.png" />
        <icon density="mdpi" src="res/icon/android/mdpi.png" />
        <icon density="hdpi" src="res/icon/android/hdpi.png" />
        <icon density="xhdpi" src="res/icon/android/xhdpi.png" />
    </platform>
    <preference name="Orientation" value="portrait" />
    <preference name="StatusBarOverlaysWebView" value="false" />
    <preference name="StatusBarBackgroundColor" value="#667eea" />
</widget>
```

### Шаг 5: Тестирование и сборка

Теперь в командной строке выполните:

```bash
# Убедитесь, что вы в папке проекта GeoCalcApp

# Тестирование в браузере
cordova run browser

# Сборка для Android
cordova build android

# Запуск на эмуляторе Android
cordova emulate android

# Запуск на подключенном устройстве
cordova run android
```

### 📝 Важные замечания:

1. **Замените ФИО**: В файле `index.html` найдите строку `[Ваше ФИО]` и замените на ваше полное имя.

2. **Для Android**: Убедитесь, что установлены:
   - Java JDK
   - Android Studio
   - Android SDK
   - Переменные среды настроены (`ANDROID_HOME`, `JAVA_HOME`)

3. **Структура проекта** должна быть:
```
GeoCalcApp/
├── config.xml
├── www/
│   ├── index.html
│   ├── css/
│   │   └── index.css
│   └── js/
│       └── index.js
├── platforms/
└── plugins/
```

### 🎨 Уникальные особенности приложения:

✅ Анимированный логотип  
✅ Градиентный дизайн в фиолетовых тонах  
✅ Модальное окно с информацией об авторе  
✅ Единицы измерения в результатах (см, см², см³)  
✅ Тактильная обратная связь (вибрация)  
✅ Улучшенная визуализация с эмодзи  

Готово! Теперь у вас есть полноценное мобильное приложение на Apache Cordova! 🚀
